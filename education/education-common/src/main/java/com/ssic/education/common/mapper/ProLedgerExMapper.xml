<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ssic.education.common.mapper.ProLedgerExMapper" >
	<select id="selectSupplierByReceiverId" resultType="com.ssic.education.common.dto.ProSupplierDto" parameterType="java.lang.String">
	   SELECT a.id,a.supplier_name as supplierName,a.address,a.supplier_type as supplierType,a.business_license as businessLicense, 
	   a.organization_code as organizationCode,a.corporation,a.contact_way as contactWay,a.longitude,a.latitude,
	   a.last_update_time as lastUpdateTime,a.create_time as createTime,a.stat
	   FROM t_pro_supplier a 
	   LEFT JOIN t_pro_ledger b on b.supplier_id = a.id 
	   WHERE a.stat =1 and b.stat = 1 
	   <include refid="where_supplier_bypage" />
	   GROUP BY a.id
	   ORDER BY a.create_time desc
       limit  #{page.startNum}, #{page.pageSize}
	</select>
	
	<select id="countSupplier" resultType="java.lang.Long" parameterType="java.lang.String">
	   SELECT count(*) from (SELECT a.id, COUNT(*) counts 
	   FROM t_pro_supplier a 
	   LEFT JOIN t_pro_ledger b on b.supplier_id = a.id  
	   WHERE a.stat =1 and b.stat = 1 
	   <include refid="where_supplier_bypage" />
	    GROUP BY a.id) t
	</select>
	
	<sql id="where_supplier_bypage">
		<if test="receiverId != null and receiverId != ''">
			and b.receiver_id = #{receiverId}
	   </if>
	</sql>
</mapper>

